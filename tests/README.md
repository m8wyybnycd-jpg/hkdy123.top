/**
 * Simple test runner script — runs all tests using Node.js built-in test runner.
 *
 * Usage:
 *   node --experimental-strip-types --test tests/*.test.ts
 *
 * Or with tsx (if installed):
 *   npx tsx --test tests/*.test.ts
 */

// This file exists to document how to run the tests.
// The actual test files use node:test and node:assert.
